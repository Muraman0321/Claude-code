# Glider Flight Analyzer — 機能仕様書

> 妻沼グライダー滑空場 (36.2114°N, 139.4189°E) を拠点とする  
> グライダーフライトの記録・分析 Web アプリケーション

---

## 1. システム構成

| 層 | 技術 |
|---|---|
| バックエンド | Python 3.11 / FastAPI / SQLAlchemy ORM / SQLite |
| フロントエンド | React 18 / TypeScript / Vite / Recharts / Leaflet |
| 気象データ | Open-Meteo Historical Forecast API（無料・APIキー不要） |
| ファイル形式 | IGC（FAI 標準フライトレコーダ形式） |

---

## 2. データモデル

### 2-1. Flight（フライト）

| カラム | 型 | 説明 |
|---|---|---|
| id | INTEGER PK | 自動採番 |
| filename | VARCHAR | 元ファイル名（正規化後） |
| pilot | VARCHAR | 選手名（大文字統一） |
| aircraft | VARCHAR | 機体番号（大文字統一） |
| remarks | VARCHAR | 備考（ファイル名の残余ブロック） |
| flight_date | DATE | フライト日 |
| started_at / ended_at | DATETIME | UTC 開始・終了時刻 |
| duration_s | FLOAT | 飛行時間（秒） |
| total_distance_km | FLOAT | 累計飛行距離（km） |
| straight_distance_km | FLOAT | 離着陸間の大円距離（km） |
| max_altitude_m | INTEGER | 最高高度（m・GPS高度優先） |
| min_altitude_m | INTEGER | 最低高度（m） |
| altitude_gain_m | FLOAT | 累積上昇高度（m） |
| max_climb_rate_ms | FLOAT | 最大瞬間上昇率（m/s） |
| avg_climb_in_thermals_ms | FLOAT | サーマル内平均上昇率（m/s） |
| avg_ground_speed_kmh | FLOAT | 平均対地速度（km/h） |
| max_ground_speed_kmh | FLOAT | 最大対地速度（km/h） |
| best_glide_ratio | FLOAT | 最大滑空比（L/D） |
| thermal_count | INTEGER | サーマル検出数 |
| thermal_time_s | FLOAT | サーマル旋回合計時間（秒） |
| cruise_time_s | FLOAT | クルーズ合計時間（秒） |
| start_latitude / start_longitude | FLOAT | 離陸地点座標 |
| weather_* | FLOAT / VARCHAR | 気象スナップショット（後述） |
| raw_igc | TEXT | 元 IGC ファイル全文（再解析用） |

一意制約: `(filename, pilot)` ペア — 同一ファイル・同一選手の重複取込みを防止。  
大文字小文字無視で比較（`func.upper()` を使用）。

### 2-2. GpsFix（GPS フィックス）

1秒サンプリングの B レコード 1 行が 1 行に対応。

| カラム | 型 | 説明 |
|---|---|---|
| flight_id | FK | フライト |
| seq | INTEGER | シーケンス番号（0-origin） |
| timestamp | DATETIME | UTC タイムスタンプ |
| latitude / longitude | FLOAT | 十進度 |
| altitude_m | INTEGER | GPS 高度（m） |
| pressure_altitude_m | INTEGER | 気圧高度（m） |
| ground_speed_kmh | FLOAT | 平滑化対地速度 |
| climb_rate_ms | FLOAT | 平滑化上昇率 |

### 2-3. ThermalRecord（サーマル）

| カラム | 型 | 説明 |
|---|---|---|
| flight_id | FK | フライト |
| start_time / end_time | DATETIME | UTC 開始・終了 |
| duration_s | FLOAT | 継続時間（秒） |
| altitude_gain_m | FLOAT | 高度獲得（m） |
| avg_climb_rate_ms | FLOAT | 平均上昇率（m/s） |
| center_lat / center_lon | FLOAT | 重心座標 |
| start_lat / start_lon | FLOAT | 旋回開始座標 |
| end_lat / end_lon | FLOAT | 旋回終了座標 |

---

## 3. ファイル取込み

### 3-1. IGC ファイル解析（`igc_parser.py`）

- H レコード: 日付（HFDTE）・選手名（HFPLT）・機体型式（HFGTY）・機体 ID（HFGID）を抽出
- B レコード: 1秒単位 GPS フィックスを抽出
  - 座標: DDMMmmmN/S, DDDMMmmmE/W 形式を十進度に変換
  - UTC 日付をまたぐフライト（day_offset）に対応
  - 気圧高度が 0 のとき GPS 高度にフォールバック

### 3-2. ファイル名パーサー（`filename_parser.py`）

#### 正規ファイル名形式

```
yy.mm.dd_<機体番号>_<選手名>[_<備考>].igc
```

#### セマンティック判定ルール

アンダースコアで分割した各ブロックに対して順序非依存で判定する。

| 優先順位 | 判定条件 | 解釈 |
|---|---|---|
| 1 | `JA` で始まるブロック | 機体番号 |
| 2 | 英字のみのブロック（最初の該当） | 選手名 |
| 3 | 残余のブロック | 備考（`_` 結合） |

- 機体番号ブロックが存在しない場合はパース失敗
- 選手名ブロックが存在しない場合はパース失敗
- 解析結果は**すべて大文字**で返す

例:

| ファイル名 | 機体 | 選手 | 備考 |
|---|---|---|---|
| `26.04.11_JA04KH_SHIN_27*3.igc` | JA04KH | SHIN | 27*3 |
| `26.04.11_TAJIMA_JA04KH.igc` | JA04KH | TAJIMA | (なし) |
| `26.04.11_JA2408_TAJIMA_27_2.igc` | JA2408 | TAJIMA | 27_2 |

#### 自動正規化（`normalize_filename`）

よくあるファイル名の誤記を自動修正して返す（変更内容を日本語で列挙）。

| 修正項目 | 内容 |
|---|---|
| 前後の空白除去 | strip() |
| 拡張子 | `.IGC` → `.igc` |
| 大文字統一 | ファイル名本体をすべて大文字に |
| 日付フォーマット | 区切り文字（`-`, `/`, `.`, `_`, スペース）を `.` に統一 |
| 4桁年 → 2桁年 | `2026.04.11` → `26.04.11` |
| フィールド区切り | ハイフン・スペースを `_` に統一、連続 `_` を圧縮 |

### 3-3. アップロード方法

#### ① ブラウザアップロード（ドラッグ＆ドロップ）

- エンドポイント: `POST /api/upload`（multipart/form-data、複数ファイル可）
- 各ファイルを正規化→解析→メトリクス計算→DB保存
- 重複チェック: `(filename, pilot)` が一致するレコードが既に存在する場合はスキップ
- レスポンス: ファイルごとの成否・エラー・正規化内容を返す

#### ② Google Drive フォルダ一括取込み

- エンドポイント: `POST /api/import-drive-folder`（JSON: `{ url, max_files }`）
- 公開共有された Google Drive フォルダの URL を指定
- `drive_import.py` がフォルダ内の `.igc` ファイルを列挙・ダウンロード
- `max_files` の上限で取込み件数を制限（デフォルト 200 件）
- 進捗は各ファイルのアップロード結果として返す

---

## 4. フライト解析アルゴリズム（`analysis.py`）

### 4-1. フィックスメトリクス計算（`compute_fix_metrics`）

隣接フィックス間の差分から対地速度・上昇率・方位を算出し、スライディングウィンドウで平滑化する。

- **上昇率**: 気圧高度差 / 経過時間、窓幅 7 点の移動平均
- **対地速度**: 水平距離 / 経過時間、窓幅 5 点の移動平均
- **外れ値除去**: 上昇率 ±15 m/s 超、速度 400 km/h 超は 0 に置換

### 4-2. サーマル検出（`detect_thermals`）

連続した上昇フィックスをサーマルセグメントとして抽出。

**検出閾値**:

| パラメータ | 値 |
|---|---|
| 上昇率（瞬間） | > 0.1 m/s |
| 最小継続時間 | ≥ 20 秒 |
| 平均上昇率 | ≥ 0.3 m/s |
| 最小高度獲得 | > 5 m |

各サーマルに: 開始・終了時刻、継続時間、高度獲得、平均上昇率、  
重心座標（center_lat/lon）、開始座標（start_lat/lon）、終了座標（end_lat/lon）を記録。

### 4-3. フライトサマリー計算（`compute_summary`）

| 指標 | 計算方法 |
|---|---|
| 総飛行距離 | haversine 累積（GPS ジャンプ除外） |
| 大円距離 | 離陸〜着陸の haversine |
| 累積上昇高度 | 平滑化上昇率 × 正の dt の積算 |
| サーマル内平均上昇率 | 時間加重平均 |
| 最大滑空比 | 60 秒ローリングウィンドウで L/D 最大値（100 未満を有効） |
| サーマル時間 / クルーズ時間 | サーマル継続時間の合算 / 残余 |

### 4-4. 気象データ取得（`weather.py`）

- API: Open-Meteo Historical Forecast API
- フライト開始地点（lat/lon）と開始時刻から最近傍の時間帯データを取得
- 取得変数: 気温（2m）、地上風速（10m）、風向（10m）、気圧、雲量、湿度
- ネットワークエラー時は `None` を返してインジェストを中断しない
- 再取得: `POST /api/flights/{id}/refresh-weather` で再実行可能

---

## 5. データベースマイグレーション

起動時 (`init_db`) に以下を冪等に実行する。

1. **テーブル作成** (`Base.metadata.create_all`)
2. **カラム追加** (`_add_missing_columns`):  
   `ALTER TABLE ... ADD COLUMN` で既存 DB に不足カラムを追加  
   対象: flights テーブルの気象カラム群、thermals テーブルの start/end 座標
3. **大文字化** (`_uppercase_pilot_aircraft`):  
   既存レコードの `pilot`, `aircraft` を全大文字に変換（既に大文字はスキップ）

---

## 6. API エンドポイント一覧

### フライト管理

| メソッド | パス | 説明 |
|---|---|---|
| GET | `/api/flights` | フライト一覧（選手・機体・日付範囲フィルタ可） |
| GET | `/api/flights/{id}` | フライト詳細（GPS フィックス＋サーマル込み） |
| DELETE | `/api/flights/{id}` | フライト削除 |
| POST | `/api/upload` | IGC ファイルアップロード（複数可） |
| POST | `/api/import-drive-folder` | Google Drive フォルダ一括取込み |
| POST | `/api/flights/{id}/refresh-weather` | 気象データ再取得 |

### 統計・集計

| メソッド | パス | 説明 |
|---|---|---|
| GET | `/api/pilots` | 選手名一覧 |
| GET | `/api/aircraft` | 機体番号一覧 |
| GET | `/api/stats/pilots` | 選手別統計 |
| GET | `/api/stats/aircraft` | 機体別統計 |
| GET | `/api/stats/time-of-day` | 時間帯別サーマル統計（ローカル時刻） |
| GET | `/api/stats/season` | 季節別統計 |
| GET | `/api/stats/weather` | 地上風速帯別フライト統計 |

### エリア分析

| メソッド | パス | 説明 |
|---|---|---|
| GET | `/api/stats/area/sectors` | セクター分割サーマル分布 |
| GET | `/api/stats/area/grid` | グリッド分割サーマル分布 |
| GET | `/api/stats/area/block-stats` | ブロック内上昇率/下降率ヒストグラム |

### 比較マップ用

| メソッド | パス | 説明 |
|---|---|---|
| GET | `/api/tracks` | 複数フライトの間引き軌跡（lat/lon のみ） |
| GET | `/api/thermals` | 複数フライトのサーマル一覧 |

### ユーティリティ

| メソッド | パス | 説明 |
|---|---|---|
| GET | `/api/normalize-filename` | ファイル名正規化プレビュー |
| GET | `/api/health` | ヘルスチェック |

---

## 7. 画面（フロントエンド）

### 7-1. ダッシュボード (`/`)

- **フライト一覧テーブル**: 日付・選手・機体・時間・距離・最高高度・平均上昇率・最高速度・サーマル数を表示
- **フィルター**: 選手・機体・日付範囲で絞り込み
- **IGC アップロード**: ドラッグ＆ドロップまたはファイル選択（複数可）
  - アップロード結果を成功/エラーで色分け表示
  - 正規化が発生した場合は変更内容を表示
- **Google Drive 取込み**: フォルダ共有 URL を貼り付けて一括インポート
- **フライト削除**: 各行の削除ボタン（確認ダイアログあり）
- フライト名をクリックでフライト詳細ページへ遷移

### 7-2. フライト詳細 (`/flights/:id`)

- フライトサマリーカード（全指標一覧）
- **高度プロファイルチャート**: 時刻 vs. 高度（m）の折れ線グラフ（Recharts）
- **速度・上昇率チャート**: 時刻 vs. 対地速度・上昇率（色分け二軸）
- **Leaflet マップ**: GPS 軌跡の表示、サーマル区間をハイライト
- **サーマル一覧テーブル**: 開始〜終了時刻・継続時間・高度獲得・平均上昇率
- **気象情報カード**: 取込み時の気象スナップショット表示

### 7-3. フライト比較 (`/compare`)

#### フライト選択

- フライト一覧テーブル（最大 100 件選択可）
- ヘッダークリックでソート（昇順/降順トグル）
- 選手・機体フィルタ、「絞り込み結果を全選択」ボタン

#### 比較サマリーチャート

- 選択フライトを横断した棒グラフ（CompareBarChart）
- 指標: 平均上昇率 (m/s)・最大 L/D・距離 (km)・**サーマル密度 (件/h)**

#### 比較テーブル列

| 列 | 内容 |
|---|---|
| 日付 | フライト日 |
| 選手 | パイロット名 |
| 機体 | 機体番号 |
| 時間 | 飛行時間 |
| 距離 | 総飛行距離 |
| 最高高度 | GPS 最高高度 |
| 平均上昇率 | サーマル内平均 |
| 最高速度 | 対地最高速度 |
| 最大 L/D | 最大滑空比 |
| サーマル密度(件/h) | サーマル数 ÷ 飛行時間(h) |

#### 軌跡重ね合わせマップ

- チェックボックスでマップ表示をトグル
- フライトごとに異なる色の Polyline を Leaflet 上に描画
- 選択数に応じてポイント密度を自動調整（合計 ≤ 1500 点）

**サーマルオーバーレイ:**

| 設定 | 選択肢 |
|---|---|
| 表示 ON/OFF | チェックボックス |
| 形状 | 円 / **角丸長方形**（開始→終了方向） |
| 時間帯フィルタ | 全時間 / スライダーで時刻 ±1h |

- **円の半径**: 対数スケール `80 × log(1 + rate × 2)` m（最小 60 m）
- **角丸長方形**: サーマルの移動方向に沿った長方形、角丸半径 = 幅の 35%
- ツールチップ: 選手/機体・上昇率・高度獲得・ローカル時刻
- サーマル上限: 最大 1,500 件（超過時はランダムサンプリング）

**ローカル時刻推定:**  
`local_hour = (utc_hour + round(longitude / 15)) % 24`

### 7-4. 統計 (`/statistics`)

- **選手別統計テーブル**: フライト数・総時間・平均上昇率・平均速度・最大 L/D・総距離
- **機体別統計テーブル**: フライト数・平均上昇率・平均速度・最大 L/D
- **時間帯別サーマルチャート**: 時刻（ローカル 0-23 時）ごとのサーマル数・平均上昇率
- **季節別チャート**: 春/夏/秋/冬のフライト数・平均気温・平均風速（フィルタ: 選手・機体）
- **地上風速別チャート**: 風速帯ごとの平均上昇率・平均速度・平均 L/D

### 7-5. エリア分析 (`/area`)

中心: 妻沼グライダー滑空場 (36.2114°N, 139.4189°E)

#### セクターモード

- 中心から半径 R km の円を N 等分したセクター（扇形）にサーマルを集計
- **セクター数**: 4 / 6 / 8 / 9 / 12 / 16 / 24（可変）
- **半径**: スライダーで 3〜15 km
- 各セクターのラベル: 0〜(N-1) 番

#### グリッドモード

- 中心を囲む N×N のグリッドセルにサーマルを集計
- **グリッドサイズ**: 3×3 / 5×5 / 7×7（可変）
- 3×3 の場合: NW/N/NE/W/C/E/SW/S/SE ラベル
- 5×5 以上: R{行}C{列} ラベル
- **セルサイズ**: スライダーで 1〜5 km/セル

#### マップ表示

- Leaflet ポリゴンで各ブロックを描画
- 色: 平均上昇率によるヒートマップ（薄黄→橙→赤）
- ブロック中心に CircleMarker（サーマル数に比例したサイズ）
- クリックで時間帯チャートとヒストグラムを表示

#### ブロック詳細パネル（クリック後）

- 時間帯別サーマル棒グラフ（ローカル 0-23 時）
- **ヒストグラム（タブ切替）:**
  - 全データ
  - 季節別（春/夏/秋/冬）
  - 日別
- 表示内容: 上昇率（> 0）ヒストグラム・下降率（< 0）ヒストグラム

### 7-6. 気象分析 (`/weather`)

フライトデータと気象データの相関を可視化する。

#### フィルター

- 選手・機体でデータを絞り込み

#### 散布図

- X 軸: 気象変数 6 種類から選択
  - 気温 (°C)、地上風速 (km/h)、雲量 (%)、湿度 (%)、気圧 (hPa)、風向 (°)
- Y 軸: 飛行指標 9 種類から選択
  - 平均上昇率・最大 L/D・総距離・サーマル数・最高高度・平均速度・最高速度・累積上昇高度・飛行時間
- **Pearson 相関係数**と強度ラベル（弱い/中程度/強い相関）を表示

#### ビン集計バーチャート

- 選択した気象変数を固定のビン幅で区間に分け、区間ごとの Y 指標平均を棒グラフ表示

#### 相関マトリクス

- 全気象変数 × 全飛行指標の Pearson r を色付きグリッドで表示
- 正の相関: 緑系、負の相関: 赤系
- 各セルに r 値を数値表示

---

## 8. ファイル名の注意事項

### 機体番号

日本の航空機登録記号。`JA` で始まる英数字混合（例: `JA04KH`, `JA2408`）。

### 選手名

英字のみのブロック。大文字で統一される（例: `TAJIMA`, `SHIN`）。

### 備考（remarks）

機体番号でも選手名でもないブロックはすべて備考として結合される。  
コース識別子などに使用（例: `27_2`, `27*3`）。

---

## 9. パフォーマンス設計

| 対策 | 内容 |
|---|---|
| 軌跡の間引き | `/api/tracks` は `lat/lon/seq` の 3 列のみ SELECT し、フライト数 N に応じて points/flight を自動削減（合計 ≤ 1500 点） |
| サーマル上限 | `/api/thermals` は最大 1,500 件まで（超過時はランダムサンプリング） |
| 重複インジェスト防止 | 同一 `(filename, pilot)` は上書き確認なしにスキップ |
| 気象失敗耐性 | Open-Meteo のタイムアウト/エラーはインジェストを止めない |

---

## 10. 技術的制約・前提

- SQLite を使用するため大規模な同時書き込みには不向き
- Open-Meteo は過去の気象データが対象（リアルタイムフライト分析は想定外）
- ローカル時刻はタイムゾーン情報なしで経度から推定（`lon / 15` 時間）
- IGC ファイルの `H` レコードから読み取れる選手名・機体名よりもファイル名の判定を優先
